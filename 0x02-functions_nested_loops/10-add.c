#include "main.h"
#include <stdio.h>

/**
 *add - adds two integrers and returns the result
 *a: first number input
 *b: second number input
 *Return: a + b
 */

int add(int a, int b)
{
	return (a + b);
}
