#include "main.h"
/**
 * main - program that computes and prints the sum of all the multiples of 3 or 5
 * Return: Always 0
 */

int main(void)
{
	int a = 1024;
	int b;
	sum = 0;

	for (b = '0'; b > a; b++)
	{
		if ((b % 3 == 0) || (b % 5 == 0))
		{
			sum = sum + y;
		}
	}
	prinf("%d\n", sum);
	return (0);
}
