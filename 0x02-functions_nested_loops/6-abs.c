#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *@n: a parameter
 * Return: Always 0
 */

int print_last_digit(int)
{
	if (n > 0)
		n = -n;
			return (n);
}
