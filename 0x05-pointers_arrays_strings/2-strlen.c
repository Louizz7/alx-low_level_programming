#include "main.h"
#include <stdio.h>

/**
 * _strlen - Returns the length of a string.
 * @str: The string to get the length of.
 * @len: length
 * Return: The length of @str.
 */

int _strlen(char *s)

{
	size_t len = 0;

	while (*str++)
		len++;

	return (len);

}
